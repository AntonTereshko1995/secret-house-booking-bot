from telegram.ext import ConversationHandler

END = ConversationHandler.END
# Global
MENU, STOPPING, BACK, CONFIRM = map(chr, range(4))
# Type of menu
BOOKING, CANCEL_BOOKING, CHANGE_BOOKING_DATE, AVAILABLE_DATES, QUESTIONS, PRICE, GIFT_CERTIFICATE = map(chr, range(4, 11))
# AVAILABLE_DATES
GET_AVAILABLE_DATES  = map(chr, range(11))
# CANCEL_BOOKING
VALIDATE_USER, SET_BOOKING_DATE = map(chr, range(12, 14))
# CHANGE_BOOKING_DATE
SET_OLD_START_DATE, SET_NEW_START_DATE, SET_NEW_START_TIME, SET_NEW_FINISH_DATE, SET_NEW_FINISH_TIME = map(chr, range(14, 19))