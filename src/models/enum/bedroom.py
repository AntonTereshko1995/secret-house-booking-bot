"""
Bedroom enum for tracking bedroom selections.
"""
from enum import Enum


class Bedroom(Enum):
    """Enum for bedroom types."""
    WHITE = "white"
    GREEN = "green"
    BOTH = "both"
    NONE = "none"
